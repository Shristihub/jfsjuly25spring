#datasource specific properties
spring:
  datasource:
    url: jdbc:mysql://mysqlcontainer:3307/deliveryapp
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: delivery-app

jwt:
 secret: deliveryapp